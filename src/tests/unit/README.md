# src/tests/unit/

Skeleton only (ticket 077-006) — kept category for host-side unit tests that
aren't domain/scenario-specific (e.g. `robot_radio` protocol parsing). Not
populated by this ticket. See `tests/CLAUDE.md`.
