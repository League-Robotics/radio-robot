{
	"version": "2.0.0",
	"presentation": {
		"reveal": "always",
		"panel": "dedicated"
	},
	"tasks": [
		{
			"type": "shell",
			"label": "CODAL build",
			"command": "uv run python3 build.py",
			"windows": {
				"command": "uv run py build.py"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			}
		},
		{
			"type": "shell",
			"label": "CODAL clean build",
			"command": "uv run python3 build.py --clean",
			"windows": {
				"command": "uv run py build.py --clean"
			},
			"group": {
				"kind": "build",
				"isDefault": false
			}
		}
	]
}
