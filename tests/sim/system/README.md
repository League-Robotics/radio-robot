# tests/sim/system/

Skeleton only (ticket 077-006) — whole-robot scenario tests against the new
`source/` tree's simulator. Populated once the sim harness (see
`tests/sim/conftest.py`) lands in a later ticket. See `tests/CLAUDE.md`.
